//boiler plate code

#include <iostream>     //This is a preprocessor directive that includes the contents of the iostream header file. iostream stands for input-output stream and contains declarations for standard input/output operations like cin (for input) and cout (for output)
using namespace std;    // This brings the entire std (standard) namespace into the current scope.The std namespace contains all the standard library functions and objects (like cout, endl, etc.)Without this, we would need to write std::cout instead of just cout

int main(){             // This declares the main function, which is the entry point of every C++ program.int indicates that the function returns an integer (typically 0 for success).The empty parentheses () mean it takes no arguments.TThe empty parentheses () mean it takes no arguments.
  //cout<<"basic syntax"<<endl;
    return 0;           //Returns the value 0 from the main() function.In C++, returning 0 from main() typically indicates successful program executionThe semicolon ; marks the end of the statement
    
}




